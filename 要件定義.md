# ぽいっと記録 要件定義書

- 文書バージョン: 0.1
- 日付: 2026-09-25
- 公開形態: GitHub Pages 上の個人向け PWA
- リポジトリ想定: `docs/` またはリポジトリルートを Pages の公開元にする

---

## 1. 目的

発言・チャットを「ぽいっと」投げるだけで、

- 今日したこと
- 今後の課題
- 残したい芽（アイデア）

を分類し、勉強ノートのように分野ごとに積もらせる。

判断・習慣化・通知・ログインを入口にしない。タイミングが悪い日でも、投げれば残る。

### 1.1 解決したいこと

- やる気が入口になると、冷えた日に何も残らない
- 日記アプリは「今日を書く」が重い
- タスクアプリは未完了の管理が主で、学びの堆積にならない
- クラウド前提だと、開く前にアカウントと同期が来る

### 1.2 やらないこと（非目標）

- SNS 連携、公開タイムライン、フォロワー
- 習慣ストリーク、ポイント、ゲーミフィケーション
- プッシュ通知での起こし
- 他者との共有を前提にしたマルチユーザー
- サーバー側アカウント・課金

---

## 2. プロダクト定義

| 項目 | 内容 |
| --- | --- |
| 名称 | ぽいっと記録 |
| 形態 | PWA（Progressive Web App） |
| 公開 | GitHub Pages（静的ホスティング） |
| データ | 端末ローカルが正（Local-first） |
| 対象 | 個人利用（まずは自分用） |
| デバイス | スマートフォン優先、PC ブラウザ可 |

GitHub Pages は静的ファイルしか置けない。バックエンド・DB・ユーザー認証は持たない。永続化はブラウザストレージと、ユーザーが持ち出すバックアップファイルで行う。

---

## 3. 利用の基本フロー

1. サイトを開く、またはホーム画面のアイコンから開く
2. 下の入力欄に、話したこと・やったこと・続きを書く（または話す）
3. 「ぽい」で投げる
4. 文を分割し、種類とカテゴリを付与して保存する
5. 「今日」で当日の断面を見る
6. 「積む」で分野別ノートとして積み上がったものを見る
7. 必要なときだけ課題一覧、書き出し、バックアップを使う

入口はチャット。出口は記録。資格確認（今日やる？）は置かない。

---

## 4. ドメイン機能要件

優先度: Must / Should / Could / Won't

### 4.1 投下（Must）

- 複数行テキストを受け付ける
- 空送信は無視する
- 改行・「。」「！」で文を分割して記録する
- 分割できない場合は原文を1件とする
- 送信後に入力欄を空にし、フォーカスを戻せる

### 4.2 自動分類（Must）

各レコードに次を付ける。

| 項目 | 値 |
| --- | --- |
| 種類 | やった / 課題 / 芽 |
| カテゴリ | 仕事 / 家庭 / 創作 / 生活 / その他 |

初期実装はキーワード規則でよい。誤分類は手動で直せることを Must とする。

分類の初期ルール概要:

- 課題: 「たい」「ねば」「課題」「あとで」「続き」「やらな」「しなきゃ」「次は」「TODO」
- 芽: 「かも」「アイデア」「思う」「構想」「概念」など（完了表現が無いとき）
- やった: 「した」「やった」「終わ」「できた」「出した」など
- カテゴリは辞書マッチ。無ければ「その他」

### 4.3 手動補正（Must）

- 種類の切替（やった → 課題 → 芽 → やった）
- レコード削除
- ノートへ積む / 積から外す

Should:

- カテゴリの手動変更
- 本文のインライン編集

### 4.4 ビュー（Must）

| ビュー | 内容 |
| --- | --- |
| 今日 | 当日のレコード。新しい順 |
| 積む | `stacked=true` のレコードをカテゴリ別に、日付の古い順で堆積 |
| 課題 | 種類が課題のもの |
| 全部 | 全レコード |

- 課題と芽は投下時に自動で `stacked=true`
- やったことは明示的に「ノートに積む」までログ扱い

### 4.5 書き出し（Must）

- カテゴリ束をプレーンテキストでコピーできる
- 形式例:

```
# 創作
- [2026-09-25] 課題 次は場面の続き
- [2026-09-25] やった 夜に小説を少し書いた
```

Should:

- 全データの JSON エクスポート
- JSON インポート（マージ / 置換を選択）

### 4.6 日付と時間（Must）

- 端末ローカル日付で「今日」を切る（日本向けは `ja-JP` ロケール）
- 各レコードに Unix 時刻と表示用日付を持つ
- 日付変更後も過去は「積む」「全部」から見える

Could:

- 任意日の振り返りカレンダー

### 4.7 音声入力（Should）

- 対応ブラウザの Web Speech API で話すだけ投下
- 非対応環境ではボタンを出さない
- 認識結果は入力欄に入れ、ユーザーが「ぽい」する（誤認識を即保存しない）

### 4.8 今日の一文（Could）

- 当日レコードから短い要約を生成する
- サーバー無しの間はテンプレ結合でよい
- 後から外部 API を足す場合も、要約はオプトイン

### 4.9 テーマの自動浮上（Could）

- 同じ語・同じ課題が複数日に出たら「厚み」として見せる
- スコアやランキングは出さない

---

## 5. この種の Web / スマホ PWA に標準搭載する機能

GitHub Pages で個人メモ・記録系 PWA を公開するとき、一般に求められる土台。アプリ固有機能とは別に Must として扱う。

### 5.1 インストール可能であること（Must）

- Web App Manifest（`manifest.json`）
- `name` / `short_name` / `start_url` / `display: standalone`
- `background_color` / `theme_color`
- `lang: ja`
- アイコン
  - 192x192 PNG
  - 512x512 PNG
  - maskable アイコン
  - SVG は補助。iOS は PNG が必要
- iOS 向け
  - `apple-touch-icon`
  - `apple-mobile-web-app-capable`
  - `apple-mobile-web-app-status-bar-style`
  - `apple-mobile-web-app-title`
- Android / Chrome の「ホーム画面に追加」
- 可能なら modest なインストール案内（初回のみ、閉じたら再出さない）

### 5.2 オフライン動作（Must）

- Service Worker でアプリシェルをキャッシュ
- オフラインでも投下・閲覧・分類・積む ができる
- オフライン時に壊れた画面ではなく、キャッシュ済み UI を出す
- SW 更新時は次回起動で新バージョンに置き換える（`skipWaiting` + バージョン番号）
- キャッシュ名をバージョン管理する（例: `poitto-v2`）

### 5.3 レスポンシブ / モバイル UI（Must）

- `viewport-fit=cover`
- 100dvh、下部セーフエリア（`env(safe-area-inset-bottom)`）
- 入力中に仮想キーボードで送信欄が隠されない
- タップ領域は 44px 以上を目安
- 幅はスマホ単列、PC では中央 720px 程度
- 横向きでも入力・一覧が使える

### 5.4 ローカル永続化（Must）

- 初期: `localStorage`
- Should: `IndexedDB`（容量・構造化・将来の全文検索のため）
- 保存失敗時は画面上に短く知らせる（容量不足など）
- シークレットブラウザでは永続しない旨を、初回だけ静かに示す

### 5.5 バックアップ / リストア（Must）

GitHub Pages にサーバーが無いため、これがアカウント代わり。

- JSON ダウンロード
- JSON 読み込み
- エクスポート日時をファイル名に含める（例: `poitto-2026-09-25.json`）
- インポート前に件数を表示する
- 既存データを消す置換と、追記マージを分ける

Could:

- 定期リマインドはしない。設定画面に「最終バックアップ」日付を出す

### 5.6 HTTPS / 公開 URL（Must・Pages 側）

- GitHub Pages の HTTPS を使う
- パスは相対（`./index.html`, `./manifest.json`）
- `start_url` と `scope` を Pages のサブパスに合わせる  
  例: プロジェクト Pages なら `/repo-name/`
- カスタムドメインは任意

### 5.7 更新とバージョン（Must）

- 画面または設定にアプリバージョンを出す
- SW 更新を検知したら「新しい版がある。再読み込み」を出せる
- 強制自動リロードはしない（入力中に消さない）

### 5.8 プライバシー（Must）

- トラッキングなし
- 解析タグなし（入れるなら後からオプトイン）
- 入力は端末外に出さない（標準機能の範囲）
- 外部 API を足す場合は設定で切れる

### 5.9 アクセシビリティ（Should）

- ボタンに分かるラベル
- コントラストを確保
- フォーカス可視化
- `prefers-reduced-motion` で装飾を抑える

### 5.10 共有・連携（Could）

- Web Share で今日の束を出す
- OS の共有シートからテキストを受け取る（Share Target）は後回し
- URL クエリで初期入力を受け取る（`?text=`）は Could

### 5.11 標準として持たないもの

思想と衝突するため、標準搭載しない。

- プッシュ通知
- デイリーリマインダー
- ストリーク
- ログイン壁
- オンボーディング長文
- バッジの「未処理件数」で焦らせること

---

## 6. GitHub Pages として確立するための要件

### 6.1 リポジトリ構成（Must）

公開を単純にするため、静的ファイルをルートまたは `docs/` に置く。

```
/
├── index.html
├── manifest.json
├── sw.js
├── icon.svg
├── icons/
│   ├── icon-192.png
│   ├── icon-512.png
│   ├── icon-maskable-512.png
│   └── apple-touch-icon.png
├── 404.html
├── .nojekyll
├── README.md
└── 要件定義.md
```

- `.nojekyll` を置く（Jekyll に `_` 付きパスを消されないようにする）
- `404.html` は `index.html` へ戻す案内、または同一アプリを出す
- Pages の Source は `main` の `/` または `/docs`

### 6.2 パス設計（Must）

| 公開の種類 | start_url 例 |
| --- | --- |
| user/organization site（`name.github.io`） | `./` または `/` |
| project site（`name.github.io/repo/`） | `./` を徹底。絶対パス `/` は使わない |

Service Worker の `scope` は Pages の配下に閉じる。別パスの旧キャッシュを汚染しない。

### 6.3 README に書くこと（Must）

- 何をするアプリか（3行）
- ライブ URL
- ホーム画面への追加手順（iPhone / Android）
- データはブラウザ内であること
- バックアップ方法
- ローカルでの開き方

### 6.4 ライセンス（Should）

個人利用が主でも、リポジトリには LICENSE を置く（MIT 推奨）。

### 6.5 CI（Could）

- 静的チェック（HTML の相対パス、manifest の必須キー）
- PNG アイコン生成
- 現状 Must ではない

---

## 7. データモデル

レコード:

```json
{
  "id": "uuid",
  "at": 1727270000000,
  "day": "2026/09/25",
  "raw": "原文まとめて投げた文字列",
  "text": "分割後の一文",
  "kind": "did | task | idea",
  "cat": "仕事 | 家庭 | 創作 | 生活 | その他",
  "stacked": true
}
```

ストレージキー: `poitto-logs-v1`

- キーを変えるときはマイグレーションを書く
- エクスポート JSON には `exportedAt` と `version` を付ける

```json
{
  "version": 1,
  "exportedAt": "2026-09-25T15:37:00+09:00",
  "items": []
}
```

---

## 8. 画面要件

### 8.1 共通

- ヘッダー: 名称と一行コンセプト
- タブ: 今日 / 積む / 課題 / 全部
- 本文: カード一覧
- フッター固定: 入力 + 送信

カード表示:

- 種類チップ
- カテゴリチップ
- 積チップ（積んでいるとき）
- 時刻（今日以外は日付つき）
- 本文
- 操作: 積む切替 / 種類切替 / 消す

### 8.2 空状態

- 今日: 「今日はまだ空。ぽいっとでいい。」
- 課題: 「まだ課題は入ってない。投げたら残る。」
- 積む: 「まだ積もってない。課題と芽は自動で積む。」

### 8.3 設定（Should）

独立画面またはフッターから開ける最小設定。

- バックアップ書き出し
- 読み込み
- バージョン
- 全削除（確認2段階）

今はタブを増やしすぎない。設定は Could でモーダルでもよい。

---

## 9. 非機能要件

| 項目 | 基準 |
| --- | --- |
| 初回表示 | 静的1ページ。外部フォント・外部 JS CDN に依存しない |
| 動作環境 | 直近の Chrome / Safari / iOS Safari / Firefox |
| オフライン | シェルと既存データは使える |
| 容量目安 | 数万件までは IndexedDB 移行前でも破綻しない設計にする |
| セキュリティ | XSS 対策として本文はエスケープして描画 |
| 国際化 | UI は日本語固定でよい |
| パフォーマンス | 一覧は単純描画。1,000件超えたら仮想化を Could |

---

## 10. フェーズ

### v0.1 現在相当（プロトタイプ）

- 投下、分割、分類、今日 / 課題 / 全部
- 積む、書き出しコピー
- manifest + 最小 SW + SVG アイコン
- localStorage

### v0.2 GitHub Pages 公開品質（次）

- PNG アイコン一式、Apple メタ
- `.nojekyll` / `404.html` / README
- JSON バックアップ IN/OUT
- SW バージョンと更新案内
- カテゴリ手動変更
- project Pages でも動く相対パス確認

### v0.3 日常耐性

- IndexedDB
- 音声入力
- 本文編集
- 設定画面
- 日付をまたいだ「厚み」表示

### v0.4 任意

- オフライン要約
- 外部 LLM 分類（キーはユーザー手元、標準オフ）
- カスタムドメイン

---

## 11. 受け入れ条件（v0.2）

1. スマホのブラウザで開き、ホーム画面追加できる
2. スタンドアロン表示になる
3. 通信オフでも既存記録の閲覧と新規投下ができる
4. 文を投げると種類とカテゴリが付く
5. 課題が「積む」に残る
6. JSON を出して別ブラウザで戻せる
7. GitHub Pages の URL だけで他端末のブラウザからも開ける（データは端末ごと）
8. 本文に HTML を書いてもスクリプトは実行されない

---

## 12. 設計原則（要件に落とす約束）

1. 入口で判断させない
2. 完成を保存条件にしない
3. 通知で自分を起こさない
4. ログとノートを分ける（全部を神聖化しない）
5. サーバーを正にしない
6. 標準 PWA 機能は黙って動く。説明は README に書く

この原則と衝突する「便利機能」は標準搭載しない。
